O que é o SWAP?
- Swap no Linux é um **espaço em disco** que o sistema operacional usa quando a **memória RAM física (principal)** está cheia. Ele atua como uma extensão da RAM, permitindo que o sistema continue rodando, mesmo com alta demanda de memória.

